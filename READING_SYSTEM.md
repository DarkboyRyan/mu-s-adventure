# 文件与黑板阅读系统

第一关参考图里的黑板位置已接入阅读系统。Hierarchy 中的物体为 `chapter1_level1 / Blackboard`，世界坐标约为 `(53.36, 36.02)`。原来的 `FileReader` 已整理为这个黑板入口；原有 `TestDocument.asset` 仍保留，可给其他文件物体使用。

## 编辑黑板文字

1. 在 Project 窗口选择 `Assets/FileText/BlackboardDocument.asset`。
2. 修改 `File Title` 和 `Pages`。`Pages` 数组的一项对应一页，支持段落换行。
3. 进入 `chapter1`，走到黑板前；底部出现 `E - Read blackboard` 后按 E。
4. A/D 或左右方向键翻页，Esc 关闭。

示例为两页英文占位内容，沿用现有字体，不代表正式剧情。改为中文时需为阅读 UI 配置包含中文字形的 TextMeshPro 字体或 fallback；当前项目的 LiberationSans 不包含完整中文字符集。

## 行为

- 阅读期间保存并暂停 `Time.timeScale`，禁用角色移动和爬梯；关闭后恢复各自原来的状态。正在进行的跳跃和计时会在关闭后继续。
- 不重复打开已经打开的文档，不会因再次按 E 回到第一页。
- 多个可读物体的触发范围重叠时，选择离玩家最近的一个。
- 兼容玩家多个碰撞体；某一个退出不会误判整个玩家离开。
- 阅读时屏蔽门、NPC、柜子、按钮和小游戏输入。关闭当帧也不向其他交互传递按键。
- 小游戏与阅读互斥。
- 本次游玩中按文档分别记住页码。未添加磁盘存档或跨场景收藏系统。
- 面板意外隐藏、管理器禁用或销毁时会恢复暂停和控制状态。
- 无内容或必要 UI 引用缺失时不锁定角色。

## 增加其他可读物体

通过 `Create > Game > File Document` 创建文档。在场景物体上添加 `FileInteractable` 和勾选 `Is Trigger` 的 2D 碰撞体，将文档拖到 `Document`，填写 `Interaction Label`。触发范围必须延伸到角色能够站立的位置。玩家或其刚体所属物体使用 `Player` 标签。

阅读 UI 已设置为随窗口缩放，标题、正文、页码和操作提示有独立区域。内容仍为手动分页，长篇文字应拆成多页。

## 验证

运行 `python3 Tests/ReadingSystem/run.py`。需要 .NET 10 SDK，无第三方测试包；临时构建文件放在系统临时目录。脚本校验场景引用和层级，并编译实际阅读/小游戏脚本，用 Unity API 替身执行 27 项行为检查。

这些检查不模拟 Unity 物理、渲染、脚本导入或实际生命周期顺序。仍需在 Unity 6000.3.2f1 中验证：

- 站在黑板前出现提示，离开后消失；无需跳起即可触发。
- 打开、翻页、重复按 E、关闭、重新打开后的页码。
- 移动或跳跃中打开阅读：阅读时静止，关闭后继续；原有两段跳及冷却仍有效。
- 阅读中 W、空格和 E 不会触发爬梯、跳跃或附近其他交互。
- 阅读时禁用管理器或隐藏面板，确认角色和游戏时间恢复。
- 在不同窗口尺寸下检查文字和提示；Console 无新增异常。
